# A simple neural network. It learn logique operator.
import random

def sigmoid(x):
    return 1/(1+np.exp(-x))

def create_neural(layers):
	neural = {}
	node_level = 0
	
	# Creat layers
	for nodes_count in layers:
		
		# Create nodes
		for node_index in range(nodes_count)
			neural['layers'][node_level][node_index]
		
			if not node_level == 0:
		
		node_level += 1
		
def learn(data_set, learn_step):
	pass
